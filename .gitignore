node_modules
.next
/build
out

# env — keep .env.example, ignore real values
.env
.env*.local

# vercel
.vercel

# local dev store (role templates) — prod uses Vercel KV
.data

# build artifacts / logs
*.tsbuildinfo
*.log
.DS_Store
